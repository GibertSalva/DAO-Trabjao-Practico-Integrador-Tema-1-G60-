from django.apps import AppConfig

# Aca definimos la configuración de la app 'reservas'
class ReservasConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'reservas'
